# Paper-Agent 使用说明

一个本地运行的论文检索与综述写作工具。输入一个研究主题，它会自动检索论文、阅读全文、
分析文献，并生成一份可追溯的领域综述。

**不需要安装任何东西**：Python 运行环境和依赖都在包里，首次启动时自动准备。

---

## 一、准备

1. 把收到的压缩包解压到一个**普通目录**，例如 `D:\PaperAgent`。
   - 不建议放在桌面、下载目录或 OneDrive 同步目录里：路径过长可能导致依赖安装失败，
     而且网盘同步会持续扫描运行中产生的数据文件，容易出错。
2. 解压后应当能看到 `start.bat`、`src`、`front`、`config`、`tools` 等。

## 二、启动

双击 `start.bat`。

- **首次启动**会下载 Python 运行环境和项目依赖，可能需要几分钟，期间没有任何输出是正常的，
  请不要关闭窗口。
- 启动完成后会自动打开浏览器。如果没有自动打开，手动访问窗口里显示的地址
  （默认是 `http://127.0.0.1:8000/`）。

## 三、首次配置模型

第一次启动会自动打开「系统配置」页面，里面已经有两个配置好的服务商（Provider），
只差你自己的 API Key。请**按下面的顺序**操作：

1. **填 Provider 的 Key**：左侧选中一个服务商，把 API Key 粘进去，点「保存 Provider」。
   配置里已经带好一个对话用的服务商，填上它的 API Key 就行。
2. **保存智能体**：在智能体区域保存一次。名称必须是 `default_agent`，否则系统会拒绝保存
   并给出提示。
3. **点「测试连通性」**：确认返回成功，就说明配置可用了。

配置保存在 `config/model.json` 里，**重启不会丢失**。之后每次启动会直接进入工作台。

## 四、使用

在「会话工作台」里用一句话描述你想调研的方向，例如：

> 帮我调研大语言模型推理加速里的 KV Cache 压缩方法

助手的操作过程（检索、阅读、分析、写作）会实时显示在界面上，每一步的产物都可以查看和下载。

### 上传自己的论文

右侧「论文工作区」面板的右上角有一个 **「上传 PDF」** 按钮，也可以把 PDF 文件直接拖进这个面板。

上传后的论文和检索到的论文一样出现在列表里，点「精读」就能读它，生成综述时也会一起分析。

- 上传后会自动从 PDF 第一页认标题、作者、年份和摘要，认错了会弹出核对框让你改。
- 扫描版（整页是图片、读不出文字）的 PDF 会被识别出来并提示你，这类论文精读只能基于摘要，报告内容会很有限。
- 同一份文件重复上传不会重复添加。

> **注意**：上传的论文原文保存在 `data/paper_cache` 目录里。**删掉这个目录会连带删掉你上传的论文原文**，
> 只删会话不会删它们。

## 五、停止

**关闭那个命令行窗口即可停止服务。** 关闭浏览器标签页不会停止它。

## 六、常见问题

**启动时说端口被占用**
说明 8000 端口被别的程序占了。可以在启动前先执行：

```
set PAPERS_PORT=8100
start.bat
```

然后访问窗口里提示的新地址。

**重复双击 `start.bat` 会怎样**
不会启动第二个服务，会直接帮你打开已经在运行的那个页面。

**想换电脑用**
把整个目录拷过去即可，但不要把 `config/model.json` 给别人（里面有你的 API Key）。

**启动时提示前端产物不完整**
说明压缩包本身有问题（缺少构建好的界面文件），请找发布的人重新要一份。

**想对一篇论文重新精读一遍**
已经精读过的论文再点「精读」，会直接打开上次的报告，不会重新跑（检索来的论文也是这样）。
想重跑，先在工作区里把这篇删掉，再把同一个 PDF 传一次——因为文件没变，系统认得是同一篇，
之前解析好的全文还在，所以重跑会很快。

**精读时为什么多出了「正在识别论文公式」「正在识别论文表格」「正在解读论文插图」几条进度，还多花了模型调用**
这几步都是在把论文里"不是纯文字的东西"也交给模型读：

- **公式识别**：把独立成段的公式截成小图转写成 LaTeX，这样精读和问答读到的才是模型看得懂的公式写法。同一页的公式合并成一次请求，一篇 35 页的论文实测大约 5 次调用。
- **表格重排**：论文里的表格是靠"排"出来的，程序只能靠框线猜哪一格属于哪一列。遇到上下两层的表头（一层写数据集、一层写指标），猜出来的表头会把好几列的名字糊在一起——数字都在、但哪一列是哪个指标全没了。这一步把表格截图交给模型按看到的排版重新写一遍，报告里才能写出"在某个数据集上某个指标是多少"。一张表一次调用。
- **插图解读**：把论文里的插图连图注一起交给模型看一遍，让它写出每张图画了什么。折线图的趋势、结构图的模块关系这些信息只存在于图里，正文和图注都没有。为了让模型看得准，还会把它所在页的正文、以及正文里提到这张图的段落一并给它——论文里通常写着图里每条曲线、每个指标是什么意思。一次带几张图，一篇论文通常一到几次调用。

两步加起来的花费都不大。公式识别不想要的话，把 `config/system.yaml` 里 `read` 下面的 `formula_ocr` 改成 `false` 即可；插图解读没有单独的开关。

---

## 给维护者：重新打包

改了代码之后，在项目根目录执行：

```powershell
uv run python scripts/package.py
```

它会自动构建前端、内置 uv、扫描密钥并生成 `Paper-Agent-<日期>.zip`。
脚本结尾会打印压缩包清单和 SHA-256，请人工核对一遍清单里**没有** `config/model.json`。
