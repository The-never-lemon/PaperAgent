# Paper-Agent 使用说明

一个本地运行的论文检索与综述写作工具。输入一个研究主题，它会自动检索论文、阅读全文、
分析文献，并生成一份可追溯的领域综述。

**不需要安装任何东西**：Python 运行环境和依赖都在包里，首次启动时自动准备。

---

## 一、准备

1. 把收到的压缩包解压到一个**普通目录**，例如 `D:\PaperAgent`。
   - 不建议放在桌面、下载目录或 OneDrive 同步目录里：路径过长可能导致依赖安装失败，
     而且网盘同步会持续扫描运行中产生的数据文件，容易出错。
2. 解压后应当能看到 `start.bat`、`src`、`front`、`config`、`tools` 等。

## 二、启动

双击 `start.bat`。

- **首次启动**会下载 Python 运行环境和项目依赖，可能需要几分钟，期间没有任何输出是正常的，
  请不要关闭窗口。
- 启动完成后会自动打开浏览器。如果没有自动打开，手动访问窗口里显示的地址
  （默认是 `http://127.0.0.1:8000/`）。

## 三、首次配置模型

第一次启动会自动打开「系统配置」页面，里面已经有两个配置好的服务商（Provider），
只差你自己的 API Key。请**按下面的顺序**操作：

1. **填 Provider 的 Key**：左侧选中一个服务商，把 API Key 粘进去，点「保存 Provider」。
   两个服务商都要填（一个用于对话，一个用于向量检索）。
2. **保存智能体**：在智能体区域保存一次。名称必须是 `default_agent`，否则系统会拒绝保存
   并给出提示。
3. **点「测试连通性」**：确认返回成功，就说明配置可用了。

配置保存在 `config/model.json` 里，**重启不会丢失**。之后每次启动会直接进入工作台。

## 四、使用

在「会话工作台」里用一句话描述你想调研的方向，例如：

> 帮我调研大语言模型推理加速里的 KV Cache 压缩方法

助手的操作过程（检索、阅读、分析、写作）会实时显示在界面上，每一步的产物都可以查看和下载。

## 五、停止

**关闭那个命令行窗口即可停止服务。** 关闭浏览器标签页不会停止它。

## 六、常见问题

**启动时说端口被占用**
说明 8000 端口被别的程序占了。可以在启动前先执行：

```
set PAPERS_PORT=8100
start.bat
```

然后访问窗口里提示的新地址。

**重复双击 `start.bat` 会怎样**
不会启动第二个服务，会直接帮你打开已经在运行的那个页面。

**想换电脑用**
把整个目录拷过去即可，但不要把 `config/model.json` 给别人（里面有你的 API Key）。

**启动时提示前端产物不完整**
说明压缩包本身有问题（缺少构建好的界面文件），请找发布的人重新要一份。

---

## 给维护者：重新打包

改了代码之后，在项目根目录执行：

```powershell
uv run python scripts/package.py
```

它会自动构建前端、内置 uv、扫描密钥并生成 `Paper-Agent-<日期>.zip`。
脚本结尾会打印压缩包清单和 SHA-256，请人工核对一遍清单里**没有** `config/model.json`。
