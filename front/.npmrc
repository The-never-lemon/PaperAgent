; npm 12 默认 allow-remote=none。若 lockfile 的 resolved 主机和当前 registry
; 不一致（例如 npmmirror vs npmjs.org），会把普通依赖误判成 remote 包，
; 报 EALLOWREMOTE，start.bat 就地构建前端就会失败。
registry=https://registry.npmjs.org/
allow-remote=all
