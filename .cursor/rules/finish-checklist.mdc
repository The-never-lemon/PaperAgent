---
description: 改动完成后必须走收尾清单：测试、文档同步、简历红线核对、可选打包。
alwaysApply: true
---

# 收尾清单

实现完成后必须按顺序做完；任一步失败即停，改动不算完成。用户要求发版时才打包。

1. **测试**：`uv run python -m unittest discover -s test -v`。失败即停，修完重跑。
2. **文档同步**：对照矩阵逐类输出「命中/未命中」，命中就更新对应文档。
3. **简历红线**：改动触及简历声称的能力或数字时，先整读 `简历-Paper-Agent项目经历.md` 文末「附一红线清单」和「附三投递前自检」，再更新并逐条核对；有新实测数字同步「附二」。未触及则写「未触及简历，跳过」。
4. **打包**（仅用户要求发版）：`uv run python scripts/package.py`，不要手工绕过脚本内的检查。
5. **收尾小结**：测试结果、更新了哪些文档、是否打包。

## 文档同步矩阵

| 改动类型 | README.md | 使用说明.md | 简历材料 |
|---|---|---|---|
| 新功能 / 行为变化 | 功能章节 | 要更新 | 仅当构成可引用亮点时 |
| 安装 / 依赖 / 命令变化 | 快速开始 | 要更新 | — |
| 评测口径 / 指标数字 | 评测章节 | — | 要更新（先读红线） |
| 架构 / 技术栈变化 | 架构章节 | — | 要更新（先读红线） |
| 修复红线中「不能写」的点 | 如涉及 | 如涉及 | 要更新该红线条目 |
| 纯重构 / 注释 / 移动 | — | — | 仅 file:line 引用失效时 |

硬规则：更新简历前必须整读附一——每个说法都要和代码对得上。
