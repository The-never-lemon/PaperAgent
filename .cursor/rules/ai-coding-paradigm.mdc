---
description: Paper-Agent 编码三阶段范式：评审 → 实现 → 收尾。任何改动都必须走完，收尾没做完不算完成。
alwaysApply: true
---

# 编码范式：评审 → 实现 → 收尾

本项目每一次代码改动都必须遵守三阶段。任何一步没做完，不进入下一步。收尾没做完，改动不算完成。

1. **写码前评审**：简单任务切 Plan 模式；复杂任务（多文件、新功能、动架构、或触及 README / 使用说明 / 简历声称的能力）先输出评审方案，用户明确说「确认 / 开始吧」后才能写码。细则见 `review-before-code` 规则。
2. **实现**：按 `AGENTS.md` / `CLAUDE.md` 写码——每步通俗中文注释、不过度抽象、目录干净、改干净不留兼容层。用户没明确要求就不写测试。
3. **收尾**：改动完成后必须执行收尾清单（测试 → 文档同步 → 简历红线核对 → 可选打包）。细则见 `finish-checklist` 规则。

完整原文：根目录 `AI-CODING-PARADIGM.md`。
